name: Terraform

on:
  push:
    branches:
      - main
      - "feature/**"
    paths:
      - "terraform/**"
  pull_request:
    branches:
      - main
    paths:
      - "terraform/**"

  # Allow manual run from GitHub Actions UI (useful for emergency applies)
  workflow_dispatch:
    inputs:
      module:
        description: "Which module to target (ecr | infra | both)"
        required: true
        default: "both"
        type: choice
        options:
          - ecr
          - infra
          - both
      action:
        description: "Terraform action to run"
        required: true
        default: "plan"
        type: choice
        options:
          - plan
          - apply
          - destroy

permissions:
  contents: read
  pull-requests: write  # allows posting plan output as a PR comment
  id-token: write       # allows OIDC auth to AWS (no static credentials needed)

env:
  TF_VERSION: "1.7.5"
  AWS_REGION: "us-east-1"

jobs:
  # ── Detect which modules changed ─────────────────────────────────────────
  detect-changes:
    name: Detect changed modules
    runs-on: ubuntu-latest
    outputs:
      ecr:   ${{ steps.filter.outputs.ecr }}
      infra: ${{ steps.filter.outputs.infra }}
    steps:
      - uses: actions/checkout@v4

      - name: Detect changed paths
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            ecr:
              - 'terraform/ecr/**'
            infra:
              - 'terraform/infra/**'

  # ── ECR Module ───────────────────────────────────────────────────────────
  terraform-ecr:
    name: "Terraform · ECR"
    runs-on: ubuntu-latest
    needs: detect-changes
    # Run when: ECR files changed, or manual trigger targeting ecr/both
    if: |
      needs.detect-changes.outputs.ecr == 'true' ||
      (github.event_name == 'workflow_dispatch' && (inputs.module == 'ecr' || inputs.module == 'both'))
    defaults:
      run:
        working-directory: terraform/ecr

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::720035686687:role/github-actions-terraform
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform format check
        id: fmt
        run: terraform fmt -check
        continue-on-error: true

      - name: Terraform init
        id: init
        run: terraform init

      - name: Terraform validate
        id: validate
        run: terraform validate

      - name: Terraform plan
        id: plan
        run: terraform plan -no-color -out=tfplan
        continue-on-error: true

      # Post plan output to the PR as a comment
      - name: Post plan to PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        env:
          PLAN: ${{ steps.plan.outputs.stdout }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const output = `### Terraform Plan · ECR 📦
            #### Format: \`${{ steps.fmt.outcome }}\` | Init: \`${{ steps.init.outcome }}\` | Validate: \`${{ steps.validate.outcome }}\` | Plan: \`${{ steps.plan.outcome }}\`

            <details><summary>Show Plan</summary>

            \`\`\`terraform
            ${process.env.PLAN}
            \`\`\`

            </details>

            *Pushed by: @${{ github.actor }}, Action: \`${{ github.event_name }}\`*`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            });

      - name: Fail if plan failed
        if: steps.plan.outcome == 'failure'
        run: exit 1

      # Apply only on merge to main (not on PRs, not on feature branches)
      - name: Terraform apply
        if: |
          github.ref == 'refs/heads/main' &&
          github.event_name == 'push' &&
          steps.plan.outcome == 'success'
        run: terraform apply -auto-approve tfplan

      # Manual apply via workflow_dispatch
      - name: Terraform apply (manual)
        if: |
          github.event_name == 'workflow_dispatch' &&
          inputs.action == 'apply'
        run: terraform apply -auto-approve

  # ── Infra Module ─────────────────────────────────────────────────────────
  terraform-infra:
    name: "Terraform · Infra"
    runs-on: ubuntu-latest
    needs: detect-changes
    if: |
      needs.detect-changes.outputs.infra == 'true' ||
      (github.event_name == 'workflow_dispatch' && (inputs.module == 'infra' || inputs.module == 'both'))
    defaults:
      run:
        working-directory: terraform/infra

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::720035686687:role/github-actions-terraform
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform format check
        id: fmt
        run: terraform fmt -check
        continue-on-error: true

      - name: Terraform init
        id: init
        run: terraform init

      - name: Terraform validate
        id: validate
        run: terraform validate

      - name: Terraform plan
        id: plan
        run: terraform plan -no-color -out=tfplan
        continue-on-error: true

      - name: Post plan to PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        env:
          PLAN: ${{ steps.plan.outputs.stdout }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const output = `### Terraform Plan · Infra 🏗️
            #### Format: \`${{ steps.fmt.outcome }}\` | Init: \`${{ steps.init.outcome }}\` | Validate: \`${{ steps.validate.outcome }}\` | Plan: \`${{ steps.plan.outcome }}\`

            <details><summary>Show Plan</summary>

            \`\`\`terraform
            ${process.env.PLAN}
            \`\`\`

            </details>

            *Pushed by: @${{ github.actor }}, Action: \`${{ github.event_name }}\`*`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            });

      - name: Fail if plan failed
        if: steps.plan.outcome == 'failure'
        run: exit 1

      - name: Terraform apply
        if: |
          github.ref == 'refs/heads/main' &&
          github.event_name == 'push' &&
          steps.plan.outcome == 'success'
        run: terraform apply -auto-approve tfplan

      - name: Terraform apply (manual)
        if: |
          github.event_name == 'workflow_dispatch' &&
          inputs.action == 'apply'
        run: terraform apply -auto-approve

      # Manual destroy (safety: only via workflow_dispatch, never auto)
      - name: Terraform destroy (manual only)
        if: |
          github.event_name == 'workflow_dispatch' &&
          inputs.action == 'destroy' &&
          inputs.module == 'infra'
        run: terraform destroy -auto-approve
